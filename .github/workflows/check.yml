name: Clippy-Formatting-Building
on: [ push, pull_request ]

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions-rs/toolchain@v1
        with:
          toolchain: stable
          components: rustfmt
          override: true

      - name: Is the formatter happy?
        run: cargo fmt -- --check

      - name: Is Clippy happy?
        run: cargo clippy

      - name: Build!
        run: cargo build
